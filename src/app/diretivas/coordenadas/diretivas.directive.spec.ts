import { DiretivasDirective } from './diretivas.directive';

describe('DiretivasDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivasDirective();
    expect(directive).toBeTruthy();
  });
});
